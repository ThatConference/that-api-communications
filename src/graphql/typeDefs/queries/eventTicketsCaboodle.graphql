type EventTicketsCaboodleQuery {
  "All sold products with product type filter"
  all(productTypes: [ProductType]): [Profile]!
  "Sold tickets which are allocated to a member"
  allocated: String
  "Sold tickets which haven't been allocated to a member"
  unallocated: String
}
