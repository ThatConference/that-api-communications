"DataSources used by messages to get addressees"
enum MessageDataSource {
  REGISTERED
  UNALLOCATED
  ORDER_HOLDERS
  ACCEPTED
  REGRETS
  WAIT_LIST
  "All orders which have unallocated tickets or training products"
  UNALLOCATED_TICKETS
}
